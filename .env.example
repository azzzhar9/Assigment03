# =============================================================================
# Multi-Agent RAG System - Environment Configuration Template
# =============================================================================
# Copy this file to .env and fill in your actual API keys
# Command: cp .env.example .env  (or copy .env.example .env on Windows)

# =============================================================================
# OPTION 1: OpenRouter (Recommended - supports multiple models)
# =============================================================================
# Get your API key from: https://openrouter.ai/keys
OPENROUTER_API_KEY=sk-or-v1-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
OPENROUTER_BASE_URL=https://openrouter.ai/api/v1

# =============================================================================
# OPTION 2: OpenAI Direct (Alternative)
# =============================================================================
# Get your API key from: https://platform.openai.com/api-keys
# OPENAI_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
# OPENAI_BASE_URL=https://api.openai.com/v1

# =============================================================================
# Model Configuration
# =============================================================================
# For OpenRouter, use: openrouter/auto (automatically selects best model)
# For OpenAI, use: gpt-4, gpt-3.5-turbo, etc.
OPENAI_MODEL=openrouter/auto

# Embedding model (used if vector embeddings are enabled)
EMBEDDING_MODEL=text-embedding-3-small

# =============================================================================
# Langfuse Observability (Optional but Recommended)
# =============================================================================
# Sign up for free at: https://cloud.langfuse.com
# Navigate to Settings → API Keys to get your keys
LANGFUSE_PUBLIC_KEY=pk-lf-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
LANGFUSE_SECRET_KEY=sk-lf-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
LANGFUSE_HOST=https://cloud.langfuse.com

# =============================================================================
# Offline / Deterministic Mode (Optional)
# =============================================================================
# Set these to "1" to disable external API calls and run in offline mode
# Useful for testing without API keys or network connectivity

# Disable LLM calls (uses TF-IDF retrieval only)
# DISABLE_LLM=1

# Disable evaluation scoring
# DISABLE_EVALUATION=1

# Disable Langfuse tracing (avoids DNS/network calls)
# DISABLE_LANGFUSE=1

# =============================================================================
# Quick Start Examples
# =============================================================================
# 
# Full LLM Mode (requires API keys):
#   1. Fill in OPENROUTER_API_KEY or OPENAI_API_KEY above
#   2. (Optional) Fill in LANGFUSE keys for observability
#   3. Run: python -m src.multi_agent_system "What are the vacation policies?"
#
# Offline Mode (no API keys needed):
#   PowerShell:
#     $env:DISABLE_LLM="1"; $env:DISABLE_EVALUATION="1"; $env:DISABLE_LANGFUSE="1"
#     python -m src.multi_agent_system "What are the vacation policies?"
#   
#   Bash:
#     DISABLE_LLM=1 DISABLE_EVALUATION=1 DISABLE_LANGFUSE=1 \
#       python -m src.multi_agent_system "What are the vacation policies?"
#
# =============================================================================
